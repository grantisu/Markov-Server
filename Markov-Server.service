[Unit]
Description=Markov Server daemon

[Service]
DynamicUser=yes
Group=nogroup
WorkingDirectory=/var/lib/Markov-Server
ExecStart=/usr/bin/plackup -o localhost -p 5000 -E deployment -s Starman --workers=1 --max-requests=50000

[Install]
WantedBy=multi-user.target
